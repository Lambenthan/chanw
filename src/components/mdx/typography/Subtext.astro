<aside class="subtext"><slot /></aside>

<style>
  .subtext {
    font-size: calc(var(--font-size-sm) * 1.05);
    font-family: var(--font-sans);
    text-align: center;
    color: var(--color-gray-700);
    margin: var(--space-2xs) auto var(--space-m);
    max-width: 64ch;
    line-height: var(--leading-snug);
  }
</style>
