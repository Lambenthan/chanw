---
/**
 * /garden — Maggie 同款 Garden subpage：长文（essays）时间流。
 *
 * 本站 notes / patterns 两类 Maggie 原 collection 已被 rename：
 *   - notes  → news (走 src/data/news.json, 在 /news)
 *   - patterns → skills (走 src/data/skills.json, 在 /skills)
 * 所以 /garden 只查 essays collection。
 *
 * book chapters 不在这里——它们属 /library/[book]/[chapter] 路由，由 /library 索引。
 */
import Layout from "../layouts/Layout.astro";
import PageWrapper from "../components/layouts/PageWrapper.astro";
import Title2 from "../components/mdx/typography/Title2.astro";
import TitleWithCount from "../components/layouts/TitleWithCount.astro";
import { Icon } from "astro-icon/components";
import { getCollection } from "astro:content";
import { extractBaseSlug } from "../utils/versionUtils";

const essays = await getCollection("essays", ({ data }) => !data.draft);

const items = essays
	.map((e) => ({
		title: e.data.title,
		description: e.data.description,
		updated: e.data.updated,
		href: `/${extractBaseSlug(e.id)}`,
	}))
	.sort((a, b) => {
		const ad = a.updated ?? new Date(0);
		const bd = b.updated ?? new Date(0);
		return new Date(bd).getTime() - new Date(ad).getTime();
	});

const fmtDate = (d?: Date) =>
	d
		? new Date(d).toLocaleDateString("en-CA", {
				year: "numeric",
				month: "short",
				day: "numeric",
			})
		: "";
---

<Layout
	title="The Garden — Chanw"
	desc="一座持续生长的学术花园 —— 长文、笔记、研究随想慢慢长出来。"
>
	<PageWrapper>
		<header style="margin-bottom: var(--space-xl);">
			<TitleWithCount posts={items}>
				<div class="back-link" slot="header-right">
					<a href="/">
						<Icon name="heroicons:arrow-left" size={16} />
						<span> Back to Home </span>
					</a>
				</div>
				The Garden
			</TitleWithCount>
			<Title2>
				一座持续生长的学术花园 —— 长文、笔记、研究随想慢慢长出来。
			</Title2>
		</header>

		{
			items.length === 0 ? (
				<div class="empty-state">
					<p>花园刚清出场地，还没栽东西。长文与研究随想陆续填入。</p>
					<p>书稿章节在 <a href="/library">/library</a>。</p>
				</div>
			) : (
				<section class="stream">
					{items.map((it) => (
						<a class="entry" href={it.href}>
							<div class="entry-meta">
								<time>{fmtDate(it.updated)}</time>
							</div>
							<h3 class="entry-title">{it.title}</h3>
							{it.description && <p class="entry-desc">{it.description}</p>}
						</a>
					))}
				</section>
			)
		}
	</PageWrapper>
</Layout>

<style>
	.back-link {
		display: inline-flex;
		align-items: center;
		height: 100%;
		position: relative;
		left: -2rem;
	}

	@media (max-width: 1440px) {
		.back-link {
			left: 0;
		}
	}

	.back-link a {
		display: inline-flex;
		align-items: center;
		gap: var(--space-3xs);
		color: var(--color-gray-600);
		font-family: var(--font-sans);
		font-size: var(--font-size-sm);
		transition: color 0.2s ease;
	}

	.back-link a:hover {
		color: var(--color-bright-crimson);
	}

	.empty-state {
		padding: var(--space-xl) 0;
		color: var(--color-gray-600);
		font-family: var(--font-body);
		font-size: var(--font-size-base);
		line-height: 1.7;
	}

	.empty-state p {
		margin: 0 0 var(--space-s);
	}

	.empty-state a {
		color: var(--color-sea-blue);
		text-decoration: none;
		border-bottom: 1px solid var(--color-tinted-cream);
		transition: border-color 0.2s ease;
	}

	.empty-state a:hover {
		border-bottom-color: var(--color-sea-blue);
	}

	.stream {
		display: flex;
		flex-direction: column;
	}

	.entry {
		display: block;
		padding: var(--space-s) 0;
		border-bottom: 1px solid var(--color-tinted-cream);
		text-decoration: none;
		color: inherit;
		transition: background-color 0.2s ease;
	}

	.entry:hover {
		background: color-mix(in srgb, var(--color-sea-blue) 4%, transparent);
	}

	.entry-meta {
		display: flex;
		align-items: baseline;
		gap: var(--space-xs);
		margin-bottom: var(--space-3xs);
		font-family: "TNR Numerals", "Times New Roman", serif;
		font-size: var(--font-size-sm);
		color: var(--color-gray-500);
	}

	.entry-meta time {
		font-variant-numeric: tabular-nums;
	}

	.entry-title {
		font-family: var(--font-serif);
		font-size: calc(var(--font-size-base) * 1.15);
		font-weight: 500;
		margin: 0 0 var(--space-3xs);
		color: var(--color-gray-800);
		line-height: 1.3;
	}

	.entry:hover .entry-title {
		color: var(--color-sea-blue);
	}

	.entry-desc {
		font-size: var(--font-size-base);
		line-height: 1.6;
		color: var(--color-gray-700);
		margin: 0;
	}
</style>
